// The actual profile experience lives in components/profile/ProfileContent —
// shared with the public /profile route (plain 'user' role reaches their
// profile there instead of inside the admin shell). See app/profile/page.js.
export { default } from '@/components/profile/ProfileContent'
